using System;

class Program
{
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager();
        goalManager.Start(); // Start the goal manager, which will display the menu and handle user input
    }
}